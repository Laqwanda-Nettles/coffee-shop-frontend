import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Add to Cart"
  }
};

export const Order = {
  args: {
    label: "Order Now"
  }
};