import CartPreview from './CartPreview';

const meta = {
  component: CartPreview,
};

export default meta;

export const Default = {
  args: {
    product: {
      "name": "Espresso",
      "imageUrl": "../../espresso.jpg"
    }
  }
};